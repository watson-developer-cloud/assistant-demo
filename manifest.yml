---
declared-services:
  conversation-service:
    label: watson_conversation
    plan: standard
applications:
- name: watson-conversation-redesign
  path: .
  command: npm start
  memory: 512M
  services:
  env:
    NODE_ENV: production
